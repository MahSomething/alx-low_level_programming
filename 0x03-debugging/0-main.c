#include "main.h"

/**
 * main - tests function prints if integer positive or negative
 * Return: 0
 */

int main(void)
{
char c = '\n';
int i;
i = 0;
piositive_or_negative(i);
printf("%c", c);
return (0);
}
