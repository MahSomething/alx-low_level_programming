#include "main.h"
/**
 * main - tests function prints if integer positive or negative
 * Return: 0
 */
int main(void)
{
char c = '\n';
int i;
if (i == 0)
{
positive_or_negative(i);
putchar(c);
}
return (0);
}
