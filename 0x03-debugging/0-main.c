#include "main.h"
/**
* main - tests function that prints if integer is 0
* @i:  - will pass the value to function to be checked
* Return: 0
*/
void positive_or_negative(int i);

int main(void)
{
int i = 0;
positive_or_negative(i);
return (0);
}
