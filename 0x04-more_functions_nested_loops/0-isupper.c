#include "main.h"
/**
* _isupper - is a function used to check and return for uppercase letters
* @c: is used to receive the value to be checked by the _isupper()
* Return: zero
*/
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
return (0);
}
