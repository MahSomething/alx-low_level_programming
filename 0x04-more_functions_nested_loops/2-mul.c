/**
* mul - A function to multiply 2 digits
* @a: is the variable that receives the 1st value
* @b: is the variable that receives the 2nd value
* Return: the Multiplication of a and b
*/
int mul(int a, int b)
{
	return (a * b);
}
